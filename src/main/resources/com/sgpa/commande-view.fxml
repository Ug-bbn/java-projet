<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Font?>

<ScrollPane xmlns="http://javafx.com/javafx/21" xmlns:fx="http://javafx.com/fxml/1"
            fx:controller="com.sgpa.controller.CommandeController"
            fitToWidth="true" fitToHeight="true"
            styleClass="content-area">
<VBox spacing="12">
    <padding><Insets top="18" right="18" bottom="18" left="18"/></padding>

    <!-- Header -->
    <HBox alignment="CENTER_LEFT" spacing="15">
        <Label text="Commandes" styleClass="section-title"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Button text="🔄 Actualiser" onAction="#handleActualiser" styleClass="button"/>
    </HBox>

    <!-- ============ CRÉER UNE COMMANDE ============ -->
    <VBox spacing="10" styleClass="details-panel">
        <Label text="📦 Nouvelle Commande" styleClass="label-bold">
            <font><Font name="System Bold" size="15"/></font>
        </Label>

        <!-- Fournisseur + Articles côte à côte -->
        <HBox spacing="15">
            <!-- Colonne gauche : Fournisseur -->
            <VBox spacing="8" HBox.hgrow="NEVER" minWidth="320" prefWidth="320">
                <Label text="Fournisseur" styleClass="label-bold"/>
                <ComboBox fx:id="cmbFournisseur" maxWidth="Infinity"/>
                <HBox spacing="5">
                    <Button text="Modifier" onAction="#handleModifierFournisseur" styleClass="button"/>
                    <Button text="Supprimer" onAction="#handleSupprimerFournisseur" styleClass="button-danger"/>
                </HBox>
            </VBox>

            <Separator orientation="VERTICAL"/>

            <!-- Colonne droite : Ajout d'article -->
            <VBox spacing="8" HBox.hgrow="ALWAYS">
                <Label text="Ajouter un article" styleClass="label-bold"/>
                <HBox spacing="10" alignment="CENTER_LEFT">
                    <ComboBox fx:id="cmbMedicament" prefWidth="220" HBox.hgrow="ALWAYS"/>
                    <Label text="Qté:"/>
                    <TextField fx:id="txtQuantite" promptText="50" prefWidth="70"/>
                    <Label text="P.U.:"/>
                    <TextField fx:id="txtPrixUnitaire" promptText="12.50" prefWidth="80"/>
                    <Button text="➕ Ajouter" onAction="#handleAjouterArticle" styleClass="button-success"/>
                </HBox>
            </VBox>
        </HBox>

        <Separator/>

        <!-- Tableau articles en cours -->
        <TableView fx:id="tableArticles" prefHeight="130" styleClass="table-view">
            <placeholder><Label text="Aucun article ajouté. Sélectionnez un médicament ci-dessus."/></placeholder>
            <columns>
                <TableColumn fx:id="colArticleMedicament" text="Médicament" minWidth="200"/>
                <TableColumn fx:id="colArticleQuantite" text="Quantité" minWidth="80" prefWidth="90"/>
                <TableColumn fx:id="colArticlePrix" text="Prix unitaire (€)" minWidth="100" prefWidth="120"/>
                <TableColumn fx:id="colArticleTotal" text="Total (€)" minWidth="100" prefWidth="120"/>
            </columns>
            <columnResizePolicy><TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/></columnResizePolicy>
        </TableView>

        <HBox spacing="10" alignment="CENTER_RIGHT">
            <Button text="❌ Retirer sélection" onAction="#handleRetirerArticle" styleClass="button-danger"/>
            <Region HBox.hgrow="ALWAYS"/>
            <Label fx:id="lblTotalCommande" text="" styleClass="label-bold"/>
            <Button text="✅ Créer Commande" onAction="#handleCreerCommande" styleClass="button-success"/>
        </HBox>
    </VBox>

    <!-- ============ LISTE DES COMMANDES ============ -->
    <VBox spacing="8" VBox.vgrow="ALWAYS" styleClass="details-panel">
        <HBox alignment="CENTER_LEFT" spacing="10">
            <Label text="Commandes enregistrées" styleClass="label-bold">
                <font><Font name="System Bold" size="15"/></font>
            </Label>
            <Region HBox.hgrow="ALWAYS"/>
            <Button text="Recevoir Commande" onAction="#handleRecevoirCommande" styleClass="button-success"/>
        </HBox>

        <TableView fx:id="tableCommandes" VBox.vgrow="ALWAYS" prefHeight="250">
            <placeholder><Label text="Aucune commande enregistrée."/></placeholder>
            <columns>
                <TableColumn fx:id="colId" text="N°" minWidth="50" prefWidth="60"/>
                <TableColumn fx:id="colFournisseur" text="Fournisseur" minWidth="160"/>
                <TableColumn fx:id="colDate" text="Date" minWidth="140" prefWidth="160"/>
                <TableColumn fx:id="colStatut" text="Statut" minWidth="100" prefWidth="120"/>
                <TableColumn fx:id="colNbArticles" text="Articles" minWidth="70" prefWidth="80"/>
                <TableColumn fx:id="colTotal" text="Total (€)" minWidth="100" prefWidth="120"/>
            </columns>
            <columnResizePolicy><TableView fx:constant="CONSTRAINED_RESIZE_POLICY"/></columnResizePolicy>
        </TableView>
    </VBox>

    <!-- ============ FOURNISSEURS (section rétractable) ============ -->
    <TitledPane text="➕ Ajouter un nouveau fournisseur" expanded="false">
        <VBox spacing="10" styleClass="details-panel">
            <GridPane hgap="10" vgap="10">
                <columnConstraints>
                    <ColumnConstraints minWidth="80" prefWidth="80"/>
                    <ColumnConstraints hgrow="ALWAYS"/>
                    <ColumnConstraints minWidth="80" prefWidth="80"/>
                    <ColumnConstraints hgrow="ALWAYS"/>
                </columnConstraints>

                <Label text="Nom:" GridPane.columnIndex="0" GridPane.rowIndex="0"/>
                <TextField fx:id="txtNomFournisseur" GridPane.columnIndex="1" GridPane.rowIndex="0"/>

                <Label text="Contact:" GridPane.columnIndex="2" GridPane.rowIndex="0"/>
                <TextField fx:id="txtContactFournisseur" GridPane.columnIndex="3" GridPane.rowIndex="0"/>

                <Label text="Adresse:" GridPane.columnIndex="0" GridPane.rowIndex="1"/>
                <TextField fx:id="txtAdresseFournisseur" GridPane.columnIndex="1" GridPane.rowIndex="1" GridPane.columnSpan="3"/>
            </GridPane>

            <HBox spacing="10" alignment="CENTER_LEFT">
                <Button text="Ajouter Fournisseur" onAction="#handleAjouterFournisseur" styleClass="button-success"/>
            </HBox>
        </VBox>
    </TitledPane>

</VBox>
</ScrollPane>
